# IMU Logger (ESP32-S3 + BMI270) H/W · S/W 기능 요구사항 명세서

## 1. 문서 목적

이 문서는 **ESP32-S3 + BMI270 + SD Card + DS3231** 기반의 고성능 IMU Logger를 상용 수준으로 구현하기 위한 하드웨어/소프트웨어 요구사항을 정리한 명세서입니다.
주 사용 목적은 **액션캠, 스마트폰 상단/측면, 자전거, 손, 드론 등**에 부착하여 고주파 IMU 데이터를 안정적으로 기록하고, 이후 **Gyroflow**에서 영상 안정화 데이터로 활용하는 것입니다.

참조 대상은 **Senseflow A1** 계열 장치이며, 여기서 제공하는 UX/운용 개념을 참고하되, 다음 방향으로 확장합니다.

* 더 높은 로깅 안정성
* 더 직관적인 LED 상태 피드백
* 더 강한 전원/파일 무결성 설계
* 더 체계적인 상태 머신 기반 운용
* 더 유연한 설정/웹 관리/USB 데이터 추출

---

## 2. 제품 목표

### 2.1 핵심 목표

* **BMI270 IMU 데이터를 1000Hz급으로 안정 기록**
* **SD 카드 쓰기 지연 최대 100ms 수준에도 데이터 유실 없이 버퍼링**
* **전원 차단/강제 종료 상황에서도 손실 범위를 최소화**
* **Gyroflow 연동을 고려한 타임스탬프/축 정의/메타데이터 체계 확보**
* **현장 사용자가 LED/버튼만으로도 상태를 즉시 이해할 수 있는 UX 제공**
* **USB 연결 시 저장장치처럼 접근 가능하거나, 최소한 빠른 데이터 추출 가능**
* **배터리 구동 장치로서 슬립/웨이크/동작 감지 기반 저전력 최적화**

### 2.2 사용 시나리오

* 액션캠 상단 부착 후 자전거 라이딩 IMU 로깅
* 스마트폰 리그 측면 부착 후 손떨림/이동 영상 보정용 로그 수집
* 드론/RC 플랫폼 진동 분석 및 자세 로그 수집
* 핸드헬드 테스트 기기로 짧은 반복 실험 데이터 기록

---

## 3. 상위 시스템 요구사항

### 3.1 플랫폼

* MCU: **ESP32-S3**
* 개발 환경: **PlatformIO + Arduino Core for ESP32**
* 외부 RAM: **PSRAM 사용 가능 보드 전제**
* RTOS: **FreeRTOS 활용**
* 파일시스템/SD: **SPI 기반 SD Card + SdFat 사용**
* IMU: **Bosch BMI270 (SPI)**
* RTC: **DS3231**
* UI: **Button 다수 + WS2812 RGB LED 다수**
* 연결성: USB, Wi-Fi(설정/시간동기화), 필요 시 BLE 확장 여지

### 3.2 기본 성능 목표

* 샘플링: 200 / 400 / 800 / 1000Hz 설정 가능
* 목표 운용: **1000Hz 연속 로깅 안정화**
* 로그 손실률: 정상 사용 조건에서 **0% 지향**
* SD 쓰기 정지(spike): **최대 100ms 이상 버퍼 흡수 가능**
* 파일 손상 시 영향 범위: **현재 파일 청크/세그먼트 단위로 최소화**
* 부팅 후 기록 시작까지: **사용자 체감 3~5초 이내**

---

## 4. Gyroflow 연동 관점 요구사항

## 4.1 데이터 품질 요구

* 자이로 데이터는 **낮은 지터**와 **일관된 샘플 간격**이 중요
* 타임스탬프는 가능한 한 **단조 증가(monotonic)** 해야 함
* 샘플 드롭/중복/역전 방지 필요
* IMU 축 방향 정의와 장치 장착 방향 보정 정보가 함께 관리되어야 함

### 4.2 메타데이터 요구

로그 파일 또는 헤더에 최소한 아래 정보 포함 권장:

* 펌웨어 버전
* 장치 ID / 시리얼
* 센서 모델(BMI270)
* 샘플링 주파수
* 가속도 범위 / 자이로 범위
* 디지털 필터 설정
* 장착 방향(orientation preset / custom matrix)
* RTC 기준 시각 / boot epoch / local tick 기준
* 캘리브레이션 오프셋 정보
* 파일 시작/종료 시각

### 4.3 영상 안정화 실사용 관점

Gyroflow는 자이로 기반 안정화에 의존하므로 다음이 중요합니다.

* 빠른 움직임에서는 충분히 짧은 셔터 속도 필요
* 카메라 내부 안정화(OIS/EIS/IBIS)는 가능하면 비활성화 권장
* 장치와 카메라 사이 시간 동기 기준이 명확해야 함
* 장착 방향이 바뀌어도 소프트웨어에서 쉽게 축 매핑 가능해야 함

---

## 5. 하드웨어 요구사항

## 5.1 블록 구성

* ESP32-S3
* BMI270 (SPI)
* SD Card (SPI)
* DS3231 (I2C 권장)
* WS2812 RGB LED x N
* 다중 버튼
* 배터리 입력 / 충전회로 / 3.3V 전원 레일
* USB Type-C (전원 + 데이터)
* 선택: 부저, 진동 모터, 전원 측정 ADC, 카메라 동기 GPIO

### 5.2 센서(IMU) 요구사항

* BMI270는 SPI 연결 사용
* INT1 핀을 ESP32-S3 GPIO 인터럽트 입력에 연결
* 가급적 IMU는 보드 진동/비틀림 영향을 줄이는 위치에 배치
* 기판 휨(strain), 온도 변화, 전원 노이즈가 오프셋에 미치는 영향 최소화
* IMU 주변 GND 리턴 경로를 짧고 안정적으로 설계
* 필요 시 기구적으로 폼/댐핑 구조 검토하되, 실제 motion fidelity 훼손은 피해야 함

### 5.3 저장장치 요구사항

* microSD socket 사용
* SD는 **SPI + SdFat dedicated mode** 우선 검토
* 전원 차단 시 카드 손상 최소화를 위해 충분한 디커플링 필요
* 카드 detect 스위치가 있다면 상태 감지에 활용
* 실제 상용 조건에서 카드 호환성 검증 필요

  * 일반 TLC 카드
  * 고내구성(high endurance) 카드
  * U1/U3/V30 등급 카드별 비교

### 5.4 RTC 요구사항

* DS3231 사용
* RTC 배터리 백업 포함
* Wi-Fi 접속 가능 시 NTP로 RTC 보정
* 로그 파일명/세그먼트 시각/사용자 표시 시간은 RTC 기준 사용
* RTC 불량 또는 시간 미설정 상태를 구분할 수 있어야 함

### 5.5 버튼 요구사항

최소 권장 버튼 구성:

* Power / Mode 버튼
* Record 버튼
* Up 버튼
* Down 버튼
* Function 버튼(선택)

버튼 UX 요구:

* short press / long press / multi-click 구분
* 디바운싱 확실히 구현
* 장갑 착용/실외 사용 고려한 물리감 확보
* 오동작 방지를 위한 길게 누름 전원 ON/OFF 채택 권장

### 5.6 LED 요구사항

WS2812 RGB LED 다수개를 사용하여 다음 정보를 직관적으로 표시:

* 전원 상태
* 부팅 중
* 캘리브레이션 중
* 기록 대기
* 기록 중
* 파일 flush 중
* Wi-Fi AP 모드
* USB MSC 모드
* 오류 상태(SD 없음, RTC 오류, IMU 오류, 파일 손상 위험, 저전압)
* 배터리 수준(막대형 또는 색상형)

권장 UX 예시:

* 녹색 점멸: 대기
* 적색 고정: 기록 중
* 청색 천천히 점멸: Wi-Fi 설정 모드
* 황색 빠른 점멸: 캘리브레이션 중
* 적색 빠른 점멸: 오류
* 무지개 sweep 1회: 부팅 완료

### 5.7 USB 요구사항

* ESP32-S3 네이티브 USB 사용
* USB CDC(디버그/CLI), MSC(대용량 저장장치), DFU/OTA 연계 구조 검토
* 실사용에서는 **USB 연결 시 안전하게 로깅 정지 후 MSC 전환** 정책 권장
* PC 연결 시 파일 추출이 쉬워야 함

### 5.8 전원 설계 요구사항

* 배터리 기반 사용 전제
* 입력원: Li-ion/LiPo 1셀 + 충전 IC + 3.3V regulator 구조 권장
* 피크 전류 고려: ESP32 Wi-Fi 송신, SD write, WS2812 동시 구동
* 주요 설계 포인트:

  * 저노이즈 3.3V 레일
  * 충분한 bulk cap 배치
  * SD 근처/ESP32 근처/IMU 근처 개별 디커플링
  * 배터리 저전압 감지
  * brownout 방지
  * USB 전원과 배터리 전원 전환 정책
* SD 쓰기/USB/Wi-Fi 동작 시 전압 강하 검증 필요

### 5.9 기구/실장 고려사항

* 액션캠/스마트폰 리그 장착을 위한 얇은 폼팩터
* 진동 전달은 확보하되 과도한 기판 공진은 억제
* 열 분산 고려
* microSD 교체 용이성
* LED 가시성
* 버튼 오조작 방지
* 렌즈/카메라 광축과의 orientation 관계를 사용자가 쉽게 맞출 수 있어야 함

---

## 6. 권장 핀맵 설계 원칙

> 실제 보드 핀 충돌은 사용 보드(ESP32-S3-WROOM/WROVER/Zero 계열), PSRAM 점유 핀, USB, 스트래핑 핀에 따라 달라지므로 최종 PCB 전에는 반드시 회로도/보드 variant 기준 재검증이 필요합니다.

### 6.1 핀 선정 원칙

* USB D+/D-는 ESP32-S3 네이티브 USB 핀 사용
* SPI는 가능하면 다음처럼 분리 검토

  * IMU용 SPI 버스
  * SD용 SPI 버스 또는 동일 SPI 내 CS 분리
* 고속/지속 쓰기 안정성 관점에서는 **SD 전용 SPI bus**가 유리
* IMU INT는 인터럽트 가능한 GPIO 사용
* WS2812는 RMT 지원 사용이 쉬운 GPIO 배정
* 부팅 스트래핑에 민감한 핀은 버튼/외부풀업/LED에 신중 사용
* Deep/Light Sleep wakeup 가능 핀 고려

### 6.2 예시 핀맵(개념안)

| 기능            | 권장 연결  | 비고                |
| ------------- | ------ | ----------------- |
| USB D-        | GPIO19 | ESP32-S3 네이티브 USB |
| USB D+        | GPIO20 | ESP32-S3 네이티브 USB |
| BMI270 SCK    | GPIO12 | SPI IMU           |
| BMI270 MOSI   | GPIO11 | SPI IMU           |
| BMI270 MISO   | GPIO13 | SPI IMU           |
| BMI270 CS     | GPIO10 | SPI IMU CS        |
| BMI270 INT1   | GPIO14 | 데이터 준비 인터럽트       |
| SD SCK        | GPIO36 | SPI SD            |
| SD MOSI       | GPIO35 | SPI SD            |
| SD MISO       | GPIO37 | SPI SD            |
| SD CS         | GPIO34 | SPI SD CS         |
| DS3231 SDA    | GPIO8  | I2C               |
| DS3231 SCL    | GPIO9  | I2C               |
| Button Power  | GPIO4  | wakeup 고려         |
| Button Record | GPIO5  |                   |
| Button Up     | GPIO6  |                   |
| Button Down   | GPIO7  |                   |
| WS2812 DIN    | GPIO21 | RMT 사용 권장         |
| Battery ADC   | GPIO1  | 분압 회로             |
| Buzzer/Vibe   | GPIO2  | 선택                |

### 6.3 핀맵 검증 체크리스트

* PSRAM 사용 핀과 충돌 없는지
* 부팅 스트래핑 영향 없는지
* USB 사용과 충돌 없는지
* SPI 신호선 길이/배치 적절한지
* SD와 IMU 동시 구동 시 signal integrity 문제 없는지
* wakeup 지원 핀 조건 충족하는지

---

## 7. 소프트웨어 아키텍처 요구사항

### 7.1 아키텍처 원칙

* 공통 함수화
* 객체지향/모듈화
* 상태 머신 기반 운용
* ISR 최소화, 큐/버퍼 기반 비동기 처리
* 로깅 경로는 **동적 메모리 할당 최소화**
* 장애 복구/오류 격리/진단 로그 확보
* 상용 수준 유지보수를 위한 계층 분리

### 7.2 권장 소프트웨어 계층

1. **BSP / HAL 계층**

   * GPIO, SPI, I2C, USB, RTC, LED, Button
2. **Device Driver 계층**

   * BMI270Driver
   * SdCardDriver
   * DS3231Driver
   * BatteryMonitor
3. **Service 계층**

   * ImuSamplingService
   * LoggerService
   * FileManagerService
   * CalibrationService
   * TimeSyncService
   * LedUiService
   * PowerService
   * WebConfigService
4. **Application 계층**

   * StateMachine
   * ModeController
   * CommandDispatcher
5. **Utility/Common 계층**

   * RingBuffer
   * DoubleBuffer
   * EventBus / Queue wrapper
   * CRC / checksum
   * Config manager
   * Binary codec / CSV exporter

### 7.3 권장 클래스/모듈 예시

* `AppController`
* `SystemStateMachine`
* `ImuManager`
* `ImuSamplerTask`
* `LogWriterTask`
* `FileSegmentManager`
* `RtcTimeManager`
* `PowerManager`
* `LedPatternManager`
* `ButtonManager`
* `WebServerManager`
* `UsbModeManager`
* `ConfigManager`
* `HealthMonitor`

---

## 8. 시스템 상태 머신 설계

### 8.1 상위 모드

* BOOT
* SELF_TEST
* IDLE
* CALIBRATION
* READY
* RECORDING
* FLUSHING
* WIFI_CONFIG
* USB_MSC
* ERROR
* SLEEP
* LOW_BATTERY_PROTECT

### 8.2 대표 상태 전이

* 전원 ON → BOOT → SELF_TEST
* SELF_TEST 성공 → READY
* READY + Record 버튼 → CALIBRATION 또는 즉시 RECORDING
* RECORDING 중 정지 → FLUSHING → READY
* USB 연결 감지 → 안전 정지 → USB_MSC
* 무동작 timeout → SLEEP
* motion interrupt → READY 또는 RECORDING 복귀
* SD/IMU 치명 오류 → ERROR

### 8.3 상태 머신 요구사항

* 각 상태는 entry / do / exit action 분리
* 비동기 이벤트 기반 상태 전이
* 상태 전이 로깅 필수
* LED 패턴과 상태를 1:1에 가깝게 연결
* 오류 상태는 recoverable / fatal 구분

---

## 9. FreeRTOS Task / Queue 설계

### 9.1 권장 Task 구성

1. **ImuAcquisitionTask**

   * BMI270 FIFO/DRDY 기반 샘플 획득
   * 최고 우선순위권
2. **LogAssembleTask**

   * 샘플 패킷화, 타임스탬프 보정, 버퍼 축적
3. **SdWriteTask**

   * 대형 버퍼를 SD에 순차 기록
4. **UiTask**

   * 버튼 스캔, LED 패턴, 사용자 피드백
5. **SystemTask**

   * 상태 머신, 오류 감시, 저전력 전이
6. **WebTask**

   * Wi-Fi/AP/웹 설정 처리
7. **UsbTask**

   * USB 연결 상태 및 MSC 전환 관리
8. **MaintenanceTask**

   * RTC sync, health metrics, housekeeping

### 9.2 Queue / Buffer 구조

* ISR → 샘플 ready 이벤트만 queue 전송
* 샘플 데이터는 queue보다 **lock-free ring buffer / double buffer** 우선
* 상태 이벤트, 버튼 이벤트, 오류 이벤트는 queue 사용
* SD writer는 큰 block 단위 쓰기를 유지

### 9.3 우선순위 원칙

* IMU 취득 > 로그 조립 > SD 쓰기 > 시스템 제어 > UI > 웹
* 웹서버/Wi-Fi가 로깅 실시간성에 영향 주지 않도록 코어/우선순위 분리 고려

---

## 10. 데이터 로깅 설계

### 10.1 핵심 원칙

* **실행 중 CSV 직접 기록 금지**
* **이진(binary) 형식으로 우선 저장**
* **큰 버퍼 + 순차 쓰기 + pre-allocation + segment rotation** 조합 사용
* 센서 취득과 SD 쓰기를 완전히 분리

### 10.2 샘플 구조 예시

```text
struct ImuSample {
  uint32_t tick_us;
  int16_t  gyro_x;
  int16_t  gyro_y;
  int16_t  gyro_z;
  int16_t  acc_x;
  int16_t  acc_y;
  int16_t  acc_z;
  int16_t  temp;
  uint16_t status;
}
```

### 10.3 타임스탬프 전략

권장 계층:

1. 고해상도 monotonic tick (`esp_timer_get_time()` 계열)
2. RTC 기준 wall-clock start time
3. 파일 헤더에 boot epoch 기록

즉, 각 샘플은 monotonic microsecond 기반으로 기록하고, 파일 헤더에 실시간 시각 anchor를 넣는 구조가 바람직합니다.

### 10.4 Double Buffering 요구사항

* 최소 2개 이상 대형 버퍼
* 예: active buffer / write buffer
* active buffer가 차면 write buffer로 넘기고, 즉시 다음 active buffer로 전환
* SD 쓰기 지연이 길어질 경우를 위해 3중 버퍼 또는 ring buffer 확장 가능

### 10.5 버퍼 용량 산정 예시

1000Hz, 샘플당 24~32B 가정 시:

* 초당 약 24~32KB
* 100ms stall 흡수에 약 2.4~3.2KB 이상 필요
* 실제 상용 설계에서는 여유를 크게 잡아 **수십~수백 KB급 버퍼** 권장
* PSRAM 사용 가능하더라도 **실시간 producer-consumer 경로 핵심 버퍼는 내부 RAM 우선**, 장기 스풀/보조 버퍼는 PSRAM 검토

### 10.6 파일 기록 정책

* `.bin` 형태의 연속 파일 기록
* 파일 시작 시 헤더 작성
* 본문은 고정 길이 record 또는 chunk 단위 저장
* 세그먼트 종료 시 footer/index/checkpoint 선택 적용
* 전원 차단 대비를 위해 일정 주기마다 sync/metadata checkpoint 수행

### 10.7 자동 파일 분할

* 5분 단위 자동 세그먼트 분할 권장
* 파일명 예시:

  * `IMU_20260307_101500_0001.bin`
  * `IMU_20260307_102000_0002.bin`
* 장점:

  * 전원 차단 시 피해 범위 축소
  * 파일 탐색 편의
  * PC 후처리 용이

### 10.8 사전 할당(pre-allocation)

* 파일 open 직후 예상 세그먼트 크기만큼 pre-allocate
* 가능하면 contiguous allocation 유도
* 세그먼트 종료 시 실제 길이로 truncate 또는 footer finalize
* 카드 단편화가 성능에 큰 영향을 주므로 장치 내 포맷 기능 제공 권장

### 10.9 무결성 요구사항

* 파일 헤더 magic/version/checksum 포함
* chunk 단위 CRC 선택 가능
* 정상 종료 플래그 / 비정상 종료 복구 플래그 관리
* 부팅 시 미종료 파일 scan & repair 수행

---

## 11. BMI270 운용 요구사항

### 11.1 샘플링/필터

* Gyroflow 목적이면 200~1000Hz 설정 지원
* 실사용 기본값은 1000Hz 또는 800Hz 우선 검토
* BMI270의 ODR/BW 설정은 **지터와 노이즈, aliasing, CPU 부하**를 함께 고려해 결정
* 저속 모드와 고속 모드를 프리셋으로 제공

### 11.2 FIFO/인터럽트 활용

* BMI270 FIFO 또는 DRDY 기반 구조 채택
* 폴링보다 인터럽트 기반 권장
* ISR에서는 최소 처리만 수행하고 task에서 burst read
* FIFO 오버런/워터마크 관리 필요

### 11.3 캘리브레이션

* 부팅 후 3초 정지 감지 시 자이로 바이어스 자동 추정
* 실패 시 재시도/건너뛰기 지원
* 수동 캘리브레이션 메뉴 제공
* 오프셋 값 저장/적용 구조 필요
* 온도에 따른 drift를 고려해 온도 기록 유지 권장

### 11.4 장착 방향 설정

* 사용자 프리셋 제공:

  * top-mounted
  * side-mounted-left
  * side-mounted-right
  * inverted
  * custom
* 각 orientation은 축 swap/sign/inversion 매트릭스로 관리
* 기록 원본(raw) + 논리 orientation 메타 저장 방식 권장

---

## 12. 전력 최적화 요구사항

### 12.1 저전력 모드 정책

* 장시간 미사용 시 light sleep 또는 deep sleep
* 버튼, USB 연결, BMI270 motion interrupt로 wakeup
* RTC alarm 기반 periodic wake 검토 가능

### 12.2 상태별 전력 정책

* READY: LED 저휘도, Wi-Fi off 기본
* RECORDING: CPU 고성능, Wi-Fi 제한, USB 기능 비활성
* WIFI_CONFIG: 샘플링 중단 또는 저속 제한
* SLEEP: WS2812 off, SD power gate 검토, IMU 저전력 motion detect

### 12.3 motion-based wakeup

* BMI270 내장 motion interrupt 기능을 사용해 움직임 감지 시 wake
* 자전거/드론/핸드헬드 사용에서는 false wake와 miss detection 균형 필요
* 웨이크 후 빠른 READY 복귀가 중요

### 12.4 배터리 보호

* 저전압 경고 → 로그 종료 유도 → 안전 flush → 종료
* 전압 강하 순간에도 헤더/파일시스템 손상이 없도록 hold-up time 확보 검토

---

## 13. USB / 데이터 추출 요구사항

### 13.1 USB MSC 모드

* PC 연결 시 장치를 USB 메모리처럼 인식시키는 방식 지원 검토
* 로깅 중에는 즉시 MSC 진입 금지
* 반드시 **record stop → flush → unmount → MSC expose** 순서 적용
* 호스트 연결 해제 후 파일시스템 remount 절차 필요

### 13.2 대체 접근

USB MSC가 운용상 복잡하면 다음 fallback 제공:

* USB CDC 명령으로 파일 export
* 웹 UI 다운로드
* SD 카드 직접 탈착

### 13.3 변환 유틸리티 전략

* 장치 내부 실시간 CSV 변환은 비권장
* PC 도구에서 `.bin → csv` 또는 `.bin → gyroflow compatible format` 변환
* 펌웨어와 함께 converter spec/version 관리 필요

---

## 14. Wi-Fi / 웹 설정 요구사항

### 14.1 운영 원칙

* Wi-Fi는 **기본 OFF**
* 설정이 필요할 때만 AP mode 또는 known STA 연결
* 로깅 실시간성에 영향이 없도록 recording 중 Wi-Fi 제한 또는 금지

### 14.2 웹 UI 기능

* 샘플링 속도 설정 (200/400/800/1000Hz)
* gyro range / accel range 설정
* LPF/BW 프리셋 설정
* orientation 설정
* 장치 이름 / 시간대 / 자동 슬립 시간 / LED 밝기 설정
* RTC/NTP 시간 동기화
* SD 사용량 / 파일 목록 / 펌웨어 버전 표시
* 로그 다운로드 / 삭제 / 포맷
* 캘리브레이션 실행

### 14.3 구현 전략

* AsyncWebServer 계열 사용 가능하나, 사용 라이브러리 유지보수성과 코어 호환성 검증 필요
* 정적 파일은 LittleFS/embedded assets에 저장
* REST API + 단순 SPA 구조 권장
* 설정은 JSON/NVS/LittleFS 기반 관리

---

## 15. UI/UX 요구사항

### 15.1 Senseflow A1 참조 확장 포인트

Senseflow A1의 핵심 UX 중 참고할 요소:

* 단순한 버튼 기반 탐색
* 현재 시간 / 저장상태 / 배터리 확인 용이성
* 자이로 기록 중심 모드 단순화
* SD 포맷/캘리브레이션/기본 설정 접근성

본 장치에서는 OLED 없이도 LED 중심 UX로 설계 가능하지만, 상용성 향상을 위해 소형 디스플레이 옵션도 검토할 수 있습니다.

### 15.2 LED 중심 UX 설계

LED는 상태를 “한눈에” 이해시키는 것이 목표입니다.

권장 표시 체계:

* 1번 LED: 시스템 상태
* 2번 LED: 기록 상태
* 3번 LED: 저장/오류 상태
* 4~N LED: 배터리 또는 모드 상태

### 15.3 버튼 동작 예시

* Power long: 전원 ON/OFF
* Record short: 녹화 시작/정지
* Mode short: 모드 순환
* Up/Down short: 값 조정
* Record long: Wi-Fi 설정 진입
* Mode long: 수동 캘리브레이션

---

## 16. 파일 포맷 요구사항

### 16.1 Binary log 포맷 권장 구조

```text
[FileHeader]
[MetaBlock]
[DataChunk 1]
[DataChunk 2]
...
[Footer / FinalIndex / CRC]
```

### 16.2 헤더 항목 예시

* magic
* format_version
* firmware_version
* device_id
* session_id
* rtc_start_unix
* monotonic_start_us
* sample_rate_hz
* gyro_range_dps
* accel_range_g
* orientation_mode
* calibration blob
* reserved

### 16.3 chunk 구조 예시

* chunk header
* first_sample_tick
* sample_count
* payload bytes
* chunk crc

### 16.4 복구 전략

* footer가 없더라도 chunk 단위로 최대한 salvage
* 마지막 유효 chunk까지만 복구 가능하도록 설계

---

## 17. 오류 처리 및 상용 수준 신뢰성 요구사항

### 17.1 오류 분류

* Recoverable

  * 일시적 SD busy
  * Wi-Fi 연결 실패
  * RTC 미동기화
* Semi-fatal

  * SD 없음
  * 캘리브레이션 실패
  * 낮은 저장공간
* Fatal

  * IMU 통신 실패 지속
  * 파일시스템 mount 실패 지속
  * 버퍼 오버런 지속
  * brownout 반복

### 17.2 보호 로직

* SD 쓰기 latency 감시
* 버퍼 수위 high watermark 감시
* FIFO overflow 감시
* watchdog 적용
* 오류 카운터 및 self-heal 재시도
* 치명 오류 시 안전 정지 후 LED 에러 표시

### 17.3 생산/서비스 관점 요구사항

* factory self-test mode
* SD benchmark mode
* IMU raw test mode
* RTC battery health check
* firmware version / board revision 표시
* 에러 로그 파일 별도 저장

---

## 18. 구현 최적화 전략

### 18.1 CPU / 메모리 최적화

* 실시간 로깅 경로에서 `String` 사용 최소화
* 동적 할당 최소화
* ISR 짧게 유지
* 큰 memcpy 횟수 최소화
* 패킷 구조체 정렬/packing 주의
* PSRAM은 대용량 보조 버퍼/웹자원 저장에 활용하되, 최고 실시간성 경로는 내부 RAM 우선

### 18.2 SD 성능 최적화

* contiguous pre-allocation
* large block sequential write
* dedicated SPI mode 검토
* flush/sync 주기 튜닝
* 카드별 latency benchmark 내장
* 조각난 카드보다 장치 포맷 후 사용 권장

### 18.3 동시성 최적화

* 기록 중 Wi-Fi 제한
* USB MSC와 recording 동시 금지
* LED 업데이트 빈도 제한
* UI 처리와 로깅 경로 분리

### 18.4 유지보수 최적화

* 모듈 간 인터페이스 명확화
* 단위 테스트 가능한 utility 분리
* 설정 구조 versioning
* 로깅 포맷 versioning
* PC converter와 포맷 문서 동기화

---

## 19. 권장 설정값(초기안)

### 19.1 기본 동작 프리셋

* **Normal Logging**

  * 400Hz
  * 표준 필터
  * 긴 배터리 시간
* **Gyroflow High Quality**

  * 1000Hz
  * gyro ±2000dps
  * accel ±16g
  * aggressive buffering
* **Low Power Motion**

  * motion wake only
  * 저속 대기
  * 기록 시에만 full speed

### 19.2 초기 부팅 정책

* 전원 인가
* self-test
* RTC 확인
* SD mount
* IMU init
* 3초 정지 감지 auto calibration
* READY 진입

---

## 20. 검증/시험 항목

### 20.1 기능 시험

* 버튼 입력 전부
* LED 상태 전부
* SD 삽입/미삽입/교체
* RTC 시간 유지
* Wi-Fi AP 접속
* 설정 저장/복원
* USB 연결/해제
* 파일 분할/종료/복구

### 20.2 성능 시험

* 1000Hz 30분/1시간/장시간 로깅
* SD worst-case latency 조건에서 drop 여부
* Wi-Fi 끈 상태/켜진 상태 비교
* LED 밝기 최대 시 전원 안정성
* 배터리 저전압 종료 시 데이터 보존

### 20.3 신뢰성 시험

* 강제 전원 차단
* SD 카드 제거 시나리오
* 진동/충격 환경
* 고온/저온 드리프트
* 반복 파일 생성/삭제/포맷

### 20.4 데이터 품질 시험

* 샘플 간격 jitter 측정
* drop/duplicate/overflow 통계
* orientation 적용 정확도
* calibration 전/후 drift 비교
* Gyroflow 실제 안정화 결과 비교

---

## 21. 권장 개발 단계

### Phase 1. Bring-up

* 전원/USB/버튼/LED/BMI270/DS3231/SD 개별 동작 확인

### Phase 2. Core Logging

* BMI270 취득 + binary logging + double buffer + pre-allocate 구현

### Phase 3. Reliability

* segment rotation, recovery, checksum, watchdog, latency monitor 구현

### Phase 4. UX/Power

* LED UX, sleep/wake, motion wake, battery 보호 구현

### Phase 5. Connectivity

* Wi-Fi config UI, time sync, USB MSC/CDC export 구현

### Phase 6. Production Hardening

* stress test, 호환성 테스트, 포맷 안정화, factory diagnostics 구현

---

## 22. 최종 권고안

이 장치는 단순한 “센서 읽기 보드”가 아니라, **고주파 실시간 데이터 로거 + 전원/파일 무결성 장치 + UX 제품**으로 접근해야 합니다.
따라서 구현 우선순위는 아래 순서가 적절합니다.

1. **1000Hz 안정 취득**
2. **버퍼링/SD 쓰기 무결성 확보**
3. **전원 차단 내성 및 자동 복구**
4. **상태 머신 기반 운용 안정화**
5. **LED/버튼 UX 정교화**
6. **Wi-Fi/USB/설정 기능 확장**

특히 상용 수준 완성도를 위해서는 다음 4가지를 사실상 필수로 보는 것이 좋습니다.

* **Double Buffering 이상 구조**
* **사전 할당(pre-allocation) + 순차 Binary Logging**
* **자동 파일 분할 + 복구 로직**
* **motion wake + 저전압 보호 + 안전 flush 종료**

---

## 23. 참고 사항

* ESP32-S3는 네이티브 USB device 기능과 sleep/wakeup 기능을 제공하며, USB MSC와 GPIO/light-sleep wakeup 구성이 가능합니다. ([docs.espressif.com](https://docs.espressif.com/projects/arduino-esp32/en/latest/api/usb_msc.html?utm_source=chatgpt.com))
* BMI270는 gyroscope ODR 최대 6.4kHz, accelerometer ODR 최대 1.6kHz와 on-chip motion-triggered interrupt 기능을 제공하므로 1000Hz 로깅과 motion wake 설계에 적합합니다. ([bosch-sensortec.com](https://www.bosch-sensortec.com/media/boschsensortec/downloads/datasheets/bst-bmi270-ds000.pdf?utm_source=chatgpt.com))
* SdFat는 contiguous file / preallocation 및 dedicated SPI mode 사용 시 성능 향상 이점을 명시하고 있습니다. ([github.com](https://github.com/greiman/SdFat?utm_source=chatgpt.com))

---

## 24. 다음 확장 가능 항목

* 카메라 셔터 동기 GPIO
* BLE remote control
* OLED/메모리 상태 표시
* GPS timestamp 보조 동기화
* 온도 기반 bias compensation
* PC용 bin→gyroflow 변환 도구
* 공장 보정/서비스 메뉴

---

# IMU Logger 기능 설계

## 25. 기능 설계 개요

본 장의 목적은 앞서 정리한 요구사항을 실제 구현 가능한 수준으로 구체화하는 것입니다.
즉, 단순 기능 목록이 아니라 **사용자 동작 → 내부 상태 전이 → 모듈 간 협력 → 데이터 흐름 → 예외 처리**까지 포함한 형태로 설계합니다.

기능 설계 기준은 다음과 같습니다.

* 사용자는 **복잡한 메뉴 없이 빠르게 기록을 시작/정지**할 수 있어야 함
* 시스템은 **1000Hz 로깅 실시간성**을 최우선으로 보장해야 함
* 설정, USB, Wi-Fi, 캘리브레이션은 모두 **기록 기능을 방해하지 않는 구조**여야 함
* 전원 차단, SD 지연, 센서 오류, 시간 미동기화 등의 상황에서 **안전한 degrade 동작**이 필요함

---

## 26. 제품 기능 구조도

### 26.1 상위 기능 그룹

1. **전원/부팅 관리 기능**
2. **자기진단(Self-Test) 기능**
3. **센서 초기화 및 캘리브레이션 기능**
4. **기록 준비/기록 시작/정지 기능**
5. **고속 데이터 취득 기능**
6. **버퍼링 및 파일 저장 기능**
7. **세그먼트 파일 관리 기능**
8. **시간 관리(RTC/NTP) 기능**
9. **사용자 입력(Button) 기능**
10. **상태 표시(LED) 기능**
11. **저전력/절전 복귀 기능**
12. **USB 데이터 추출 기능**
13. **Wi-Fi 웹 설정 기능**
14. **오류 감시/복구 기능**
15. **진단/공장 테스트 기능**

### 26.2 핵심 기능 우선순위

* 1순위: **센서 취득 + 버퍼링 + SD 저장 안정성**
* 2순위: **전원 차단 내성 + 파일 복구**
* 3순위: **LED/버튼 UX**
* 4순위: **Wi-Fi/USB/설정 기능**

---

## 27. 사용자 기능 설계

## 27.1 전원 ON 기능

### 목적

사용자가 장치를 켜면 최대한 짧은 시간 안에 기록 가능한 상태로 진입시킴.

### 입력

* Power 버튼 long press
* 또는 USB 연결에 따른 자동 부팅(정책 선택)

### 처리 흐름

1. PMIC/전원 레일 안정화
2. MCU boot
3. RTC/SD/IMU/GPIO/LED 초기화
4. 자기진단 수행
5. 설정 로드
6. 필요 시 자동 캘리브레이션
7. READY 상태 진입

### 출력/피드백

* 부팅 중 LED 애니메이션
* 오류 시 LED 에러 패턴
* 정상 시 READY 패턴 표시

### 실패 처리

* SD mount 실패 시 READY 대신 `DEGRADED_READY` 또는 ERROR 진입 정책 선택
* IMU init 실패 시 기록 기능 불가 상태 표시

---

## 27.2 전원 OFF 기능

### 목적

사용자 종료 요청 또는 저전압 종료 시 파일 무결성을 보장하며 안전 종료.

### 입력

* Power 버튼 long press
* 저전압 보호 이벤트
* 치명 오류 이벤트

### 처리 흐름

1. 현재 recording 여부 확인
2. recording 중이면 stop sequence 수행
3. active buffer flush 요청
4. SD sync / file finalize / unmount
5. 종료 로그 기록
6. LED 종료 패턴 후 power off 또는 deep sleep 진입

### 예외 처리

* flush 시간 초과 시 가능한 범위까지만 salvage 후 종료
* 이미 SD 오류 상태면 RAM 내 상태만 정리 후 종료

---

## 27.3 기록 시작 기능

### 목적

사용자가 Record 버튼으로 즉시 기록 시작.

### 입력

* Record 버튼 short press
* 옵션: motion trigger
* 옵션: 예약/자동 시작

### 사전 조건

* IMU 정상
* SD 정상
* 파일 생성 가능
* battery 보호 수준 이상

### 처리 흐름

1. 상태 확인 (`READY`인지 검증)
2. 필요 시 3초 auto calibration 수행 또는 calibration 유효성 확인
3. 새 세션 ID 생성
4. 새 세그먼트 파일 생성 및 pre-allocate
5. file header/meta 기록
6. double buffer 활성화
7. IMU acquisition enable
8. `RECORDING` 상태 진입

### 사용자 피드백

* LED 적색 고정 또는 pulse
* 시작 성공 tone/진동(선택)

### 실패 처리

* 파일 open/pre-allocate 실패 시 기록 진입 거부
* RTC 미동기화는 허용하되 “time uncertain” 플래그 기록

---

## 27.4 기록 정지 기능

### 목적

현재 기록 세션을 정상 종료하고 파일을 완결 상태로 저장.

### 입력

* Record 버튼 short press
* USB 연결 요청
* low battery 종료
* 사용자가 모드 전환

### 처리 흐름

1. IMU acquisition stop request
2. 잔여 FIFO read
3. active buffer close
4. write queue drain
5. SD write/sync/finalize
6. 세그먼트 footer 기록
7. session summary 갱신
8. READY 상태 복귀

### 사용자 피드백

* 적색 → 녹색 상태 전환
* flush 중 황색 점멸

---

## 27.5 자동 캘리브레이션 기능

### 목적

부팅 직후 또는 기록 직전 정지 상태를 이용해 gyro bias 보정.

### 입력

* 자동 실행
* 수동 메뉴 실행

### 처리 흐름

1. 최근 N ms 가속도/자이로 안정성 평가
2. 정지 상태 충족 시 샘플 수집
3. 축별 평균 bias 계산
4. 허용 범위 검증
5. bias 적용 및 저장
6. calibration metadata 기록

### 실패 조건

* 사용자가 장치를 움직임
* 센서 값 분산이 임계치 초과
* 시간 초과

### 실패 후 정책

* 기존 저장 bias 사용
* 또는 무보정 상태로 계속 진행하되 경고 표시

---

## 27.6 장착 방향(Orientation) 설정 기능

### 목적

상단/측면/뒤집힘 등 실제 부착 방향을 소프트웨어에서 보정.

### 사용자 설정 항목

* Top Mount
* Side Left
* Side Right
* Inverted
* Custom Axis Mapping

### 처리 흐름

1. 사용자가 프리셋 선택
2. 축 변환 매트릭스 로드
3. 실시간/저장 메타에 반영
4. 파일 헤더에 orientation 정보 저장

### 설계 원칙

* raw sensor 값 자체는 가급적 원형 유지
* logical export 또는 metadata 기반 해석 가능 구조 권장

---

## 27.7 Wi-Fi 설정 모드 기능

### 목적

스마트폰/PC로 장치 설정 변경.

### 진입 조건

* 버튼 long press
* 설정 메뉴 진입
* 부팅 시 특정 버튼 조합

### 처리 흐름

1. recording 아님을 확인
2. AP 또는 STA 모드 활성화
3. 웹 서버 start
4. 설정 조회/수정 API 제공
5. 필요 시 NTP 동기화
6. 사용 종료 후 READY 복귀 또는 저장 후 재부팅

### 제한 정책

* recording 중 Wi-Fi 설정 금지 또는 읽기 전용
* 고속 샘플링 중 AP mode 진입 금지

---

## 27.7A LittleFS 기반 설정 저장 기능

### 목적

설정값을 **LittleFS 내 JSON 파일**로 저장하여, 재부팅 후에도 일관된 사용자 환경과 시스템 동작을 유지함.

### 설계 원칙

* 설정은 **LittleFS + JSON 파일**을 기본 저장소로 사용
* 설정 로딩 실패 시 **컴파일 타임 기본값**으로 복구
* 저장 중 전원 차단에 대비해 **원본 직접 덮어쓰기 금지**
* 설정 파일은 **버전(version)** 과 **schema** 를 포함하여 migration 가능 구조로 설계
* 웹 UI, 버튼 메뉴, 공장 초기화, 자동 보정 결과 저장이 모두 동일한 ConfigManager 경로를 사용해야 함

### 저장 대상 설정 그룹

* device
* logging
* imu
* power
* ui
* wifi
* time
* calibration
* diagnostics
* file_policy

### 파일 구조 권장안

```text
/lfs/config/
  config.json
  config.tmp
  config.bak
  runtime.json
  calib.json
  stats.json
```

### 파일별 역할

* `config.json`: 사용자 기본 설정의 주 저장 파일
* `config.tmp`: atomic save 중간 파일
* `config.bak`: 마지막 정상 설정 백업
* `runtime.json`: 마지막 상태, 최근 모드, 마지막 세션 번호 등 비핵심 런타임 상태
* `calib.json`: 자이로 bias, orientation custom matrix, calibration metadata
* `stats.json`: 부팅 횟수, 비정상 종료 횟수, max SD latency 등 서비스 진단 정보

### 저장 처리 흐름

1. 현재 메모리상의 설정 구조체를 JSON Document로 직렬화
2. `config.tmp`로 먼저 저장
3. 저장 직후 read-back 검증 수행
4. JSON parse 및 validation 재실행
5. 기존 `config.json`을 `config.bak`로 교체 또는 보관
6. `config.tmp`를 `config.json`으로 rename
7. 저장 성공 이벤트 기록

### 부팅 시 로드 흐름

1. LittleFS mount
2. `config.json` 존재 여부 확인
3. parse 및 schema/version 검증
4. 누락 키는 기본값 보충
5. 범위 초과 값은 validation으로 clamp 또는 default 대체
6. migration 필요 시 최신 구조로 변환
7. 메모리 설정 구조체 반영
8. 필요 시 auto-save로 최신 포맷 재저장

### 예외 처리

* `config.json` 손상 시 `config.bak` 복구 시도
* `config.bak`도 실패 시 factory default 사용
* LittleFS mount 실패 시 read-only safe mode 또는 default config로 부팅
* 저장 실패 시 기존 config 유지, 사용자에게 LED/웹 UI 경고 표시

### validation 요구사항

* `sample_rate_hz`: 허용값 집합(200/400/800/1000)
* `segment_minutes`: 최소/최대 범위 제한
* `gyro_range_dps`, `accel_range_g`: 센서 지원 범위만 허용
* `led_brightness`: 0~255 또는 제품 정책 범위 제한
* `timezone`: 유효 문자열 또는 preset 기반 제한
* `wifi.ap_ssid`: 길이 및 문자 검증

### 저장 정책

* 사용자가 명시적으로 저장한 경우 즉시 저장
* 자주 변하는 runtime 값은 `config.json`이 아닌 `runtime.json`에 분리 저장
* calibration 결과는 `calib.json`으로 별도 저장
* statistics는 주기적 또는 이벤트 기반 저장
* 잦은 flash write 방지를 위해 debounce/throttle 적용

### 상용 수준 권장사항

* 설정 변경은 RAM shadow copy에 먼저 반영 후 commit
* config save는 mutex로 보호
* 저장 직전/직후 checksum 또는 hash 검증 가능
* schema migration 함수는 버전별 분리 유지
* JSON 파일은 사람이 읽기 쉬운 pretty format 대신 compact format 우선, 필요 시 export만 pretty 지원

---

## 27.8 USB MSC 기능

### 목적

스마트폰/PC로 장치 설정 변경.

### 진입 조건

* 버튼 long press
* 설정 메뉴 진입
* 부팅 시 특정 버튼 조합

### 처리 흐름

1. recording 아님을 확인
2. AP 또는 STA 모드 활성화
3. 웹 서버 start
4. 설정 조회/수정 API 제공
5. 필요 시 NTP 동기화
6. 사용 종료 후 READY 복귀 또는 저장 후 재부팅

### 제한 정책

* recording 중 Wi-Fi 설정 금지 또는 읽기 전용
* 고속 샘플링 중 AP mode 진입 금지

---

## 27.8 USB MSC 기능

### 목적

PC 연결 시 파일을 쉽게 복사.

### 진입 조건

* USB 연결 감지
* 사용자가 USB mode 선택

### 처리 흐름

1. recording 여부 확인
2. recording 중이면 정지 절차 수행
3. SD unmount
4. MSC expose
5. host eject/분리 감지
6. SD remount
7. READY 복귀

### 주의

* MSC 노출 중 내부 앱이 SD에 접근하면 안 됨
* 안전한 연결/해제 상태 관리 필요

---

## 27.9 절전/자동 웨이크 기능

### 목적

배터리 사용 시간을 늘리면서 사용 시작은 빠르게 유지.

### sleep 진입 조건

* 일정 시간 무동작
* 사용자가 sleep 명령
* motion standby 모드

### wakeup 소스

* Power 버튼
* Record 버튼
* BMI270 motion interrupt
* USB 연결
* RTC alarm(선택)

### 흐름

1. 주변장치 저전력 전환
2. LED off 또는 저휘도
3. IMU motion detect만 유지
4. wake event 발생 시 시스템 복귀
5. READY 재진입 또는 auto record

---

## 28. 내부 기능 설계

## 28.1 자기진단(Self-Test) 기능

### 목적

기록 시작 전 주요 하드웨어 이상을 조기 발견.

### 점검 대상

* PSRAM 사용 가능 여부
* SD mount/read/write test
* BMI270 ID 및 init register 확인
* RTC 응답 및 시각 유효성
* 버튼 stuck 여부
* LED driver 동작
* 배터리 전압 수준

### 결과 등급

* PASS
* PASS_WITH_WARNING
* FAIL

### 동작 정책

* warning이면 기록 허용 가능
* fail이면 해당 기능 비활성 또는 ERROR 상태 진입

---

## 28.2 IMU 샘플 취득 기능

### 목적

BMI270로부터 정확하고 안정적인 샘플을 가져옴.

### 입력

* DRDY interrupt 또는 FIFO watermark interrupt

### 처리 흐름

1. ISR에서 event flag만 set
2. ImuAcquisitionTask가 wake
3. SPI burst read 수행
4. raw frame decode
5. tick timestamp 부여
6. 샘플 유효성 검사
7. sample buffer enqueue

### 유효성 검사

* sample count 이상 여부
* FIFO overflow 여부
* register status bit 확인
* timestamp monotonic check

### 산출물

* `ImuSample` 배열 또는 chunk

---

## 28.3 로그 조립 기능

### 목적

센서 샘플을 저장 가능한 block/chunk로 변환.

### 처리 흐름

1. raw sample 수신
2. orientation metadata 연결
3. calibration bias 적용 여부 결정
4. packed binary record 생성
5. active buffer에 append
6. watermark 도달 시 write-ready buffer로 swap

### 설계 포인트

* sample마다 동적 메모리 할당 금지
* 최소 복사 전략 유지
* 구조체 정렬과 endian 고려

---

## 28.4 SD 쓰기 기능

### 목적

write-ready buffer를 SD에 순차적으로 저장.

### 처리 흐름

1. write queue에서 buffer 확보
2. file open 상태 확인
3. contiguous/pre-allocated 영역에 write
4. write latency 측정
5. 임계치 초과 시 telemetry 기록
6. 주기적 sync
7. 완료 후 buffer recycle

### 예외 처리

* 일시적 쓰기 지연 → 버퍼 수위 감시
* write 실패 → 재시도 횟수 제한 후 오류 전이
* sync 실패 → current segment abort/recovery flag

---

## 28.5 파일 세그먼트 관리 기능

### 목적

장시간 로깅 중 자동 분할과 복구 용이성 제공.

### 분할 기준

* 시간 기준: 5분
* 용량 기준: 예 256MB
* 사용자가 설정 가능

### 처리 흐름

1. 현재 세그먼트 카운터 감시
2. 기준 도달 시 rotate 요청
3. old segment finalize
4. new segment pre-allocate
5. header/meta 기록
6. logging 연속성 유지

### 설계 포인트

* rotate 중 샘플 유실이 없어야 함
* rotate 순간은 buffer 여유를 충분히 확보해야 함

---

## 28.6 RTC/NTP 시간 관리 기능

### 목적

로그 파일명과 wall-clock 기준시각을 안정적으로 부여.

### 기능 항목

* RTC 읽기/쓰기
* RTC 유효 시각 판정
* NTP 동기화
* timezone 설정
* drift 보정(선택)

### 동작 정책

* RTC 유효하면 부팅 시 현재 시간 사용
* Wi-Fi 연결 성공 시 NTP 동기화 후 RTC 갱신
* RTC 무효 시 monotonic logging은 계속 가능

---

## 28.7 설정 관리 기능

### 목적

모든 시스템 설정을 **LittleFS 기반 JSON 파일**로 관리하고, 버전 업 및 오류 복구가 가능한 구조로 설계.

### 설정 그룹 예시

* sampling config
* imu range config
* file segment config
* power config
* led config
* button config
* wifi config
* orientation config
* calibration config
* time sync config
* diagnostics config

### 요구사항

* 기본값 복구 가능
* 버전 업 시 migration 가능
* 잘못된 설정값 validation 필수
* 저장 실패 시 rollback 또는 이전 값 유지
* runtime state와 persistent config 분리
* calibration, statistics, user config를 분리 저장

### 내부 구조 권장안

```text
ConfigManager
 ├─ loadAll()
 ├─ saveMainConfig()
 ├─ saveRuntimeState()
 ├─ saveCalibration()
 ├─ saveStatistics()
 ├─ restoreFactoryDefaults()
 ├─ migrateConfig()
 ├─ validateConfig()
 └─ exportConfigJson()
```

### 설정 데이터 라이프사이클

1. boot 시 LittleFS mount
2. main config load
3. migration/validation/default fill 수행
4. runtime/calibration/statistics 순차 로드
5. 시스템 각 서비스에 distribute
6. 변경 요청 발생 시 RAM shadow에 반영
7. commit 시 JSON 파일 저장
8. 성공 시 active config generation 증가

### 권장 JSON 저장 예시

```json
{
  "schema_version": 3,
  "device": {
    "name": "IMU-LOGGER-01",
    "timezone": "Asia/Seoul"
  },
  "logging": {
    "sample_rate_hz": 1000,
    "segment_minutes": 5,
    "prealloc_mb": 64,
    "sync_interval_sec": 5
  },
  "imu": {
    "gyro_range_dps": 2000,
    "accel_range_g": 16,
    "orientation": "top_mount",
    "auto_calibration": true
  },
  "power": {
    "auto_sleep_sec": 900,
    "motion_wake": true,
    "low_battery_shutdown": true
  },
  "ui": {
    "led_brightness": 32,
    "recording_led_policy": "minimal"
  },
  "wifi": {
    "enabled": false,
    "ap_ssid": "IMU-LOGGER"
  }
}
```

### ConfigManager와 타 모듈 연계

* `WebConfigService` → 설정 읽기/수정 요청
* `SystemStateMachine` → 상태 전이에 따라 수정 허용 여부 판단
* `ImuManager` → 샘플링/레인지/orientation 반영
* `LoggerService` → segment/prealloc/sync policy 반영
* `PowerManager` → auto sleep / low battery policy 반영
* `LedPatternManager` → brightness / policy 반영
* `RtcTimeManager` → timezone / NTP policy 반영

### 권장 저장 금지 항목

아래 값들은 메인 설정파일에 과도하게 자주 쓰지 않도록 분리 권장:

* 마지막 배터리 전압
* 최근 버튼 이벤트
* 현재 버퍼 수위
* 임시 오류 카운터
* 초 단위 session runtime

이 값들은 `runtime.json` 또는 RAM only 상태로 관리하는 것이 적절함.

## 28.8 상태 표시(LED) 기능

### 목적

화면 없이도 현재 상태를 명확히 전달.

### 입력

* system state
* battery level
* error severity
* storage status
* wifi/usb mode

### 출력 방식

* 색상
* 점멸 주기
* sweep/segment bar
* 복수 LED 조합

### 설계 포인트

* recording 중 과도한 LED 업데이트 금지
* 저전력 상태에서는 표시 최소화
* 심각 오류는 눈에 띄게 반복 표시

---

## 28.9 버튼 입력 처리 기능

### 목적

다수 버튼 이벤트를 신뢰성 있게 처리.

### 지원 이벤트

* press
* release
* short click
* long press
* double click
* repeat hold

### 처리 흐름

1. 주기적 scan 또는 GPIO interrupt 기반 wake
2. debounce
3. event decode
4. queue 전달
5. UI/StateMachine에서 소비

### 보호 로직

* 전원 버튼은 accidental touch 방지
* recording 중 특정 버튼 조합만 허용

---

## 28.10 배터리/전원 감시 기능

### 목적

저전압으로 인한 데이터 손상 방지.

### 기능 항목

* 배터리 전압 ADC 측정
* 충전 상태 감지(선택)
* 저전압 warning/fatal threshold 관리
* brownout event 기록

### 동작 정책

* warning 단계: LED 경고 + 사용자 알림
* critical 단계: 자동 정지 + flush + 종료

---

## 28.11 오류 감시/복구 기능

### 목적

일시적 오류는 자동 복구하고 치명 오류는 안전 정지.

### 감시 항목

* IMU no response
* FIFO overflow
* buffer overrun
* SD latency spike
* file write failure
* RTC invalid
* low battery
* heap/PSRAM watermark
* watchdog near miss

### 복구 정책

* recoverable: 재시도 후 warning 상태 유지
* semi-fatal: 기능 제한 후 사용자 경고
* fatal: recording stop 후 ERROR 진입

---

## 29. 데이터 흐름 설계

## 29.1 정상 기록 흐름

```text
BMI270 INT
→ ImuAcquisitionTask wake
→ SPI burst read
→ Raw sample decode
→ Timestamp attach
→ Sample buffer append
→ Buffer watermark hit
→ Buffer swap
→ SdWriteTask write
→ Periodic sync
→ Segment rotate (optional)
```

## 29.2 기록 종료 흐름

```text
Record stop request
→ IMU acquisition stop
→ FIFO drain
→ active buffer close
→ queued write complete
→ file footer/finalize
→ sync
→ READY
```

## 29.3 USB MSC 전환 흐름

```text
USB attach detect
→ if RECORDING then stop/finalize
→ SD unmount
→ MSC expose
→ host detach
→ SD remount
→ READY
```

---

## 30. 상태별 기능 허용 매트릭스

| 기능           | READY | RECORDING | WIFI_CONFIG | USB_MSC | ERROR |  SLEEP |
| ------------ | ----: | --------: | ----------: | ------: | ----: | -----: |
| Record Start |     O |         X |           X |       X |     X |    조건부 |
| Record Stop  |     X |         O |           X |       X |     X |      X |
| Wi-Fi 설정 변경  |     O |      제한/X |           O |       X |    제한 |      X |
| USB MSC 진입   |     O |   정지 후 가능 |           X |       O |    제한 | wake 후 |
| Calibration  |     O |         X |           O |       X |    제한 |      X |
| LED 상태 표시    |     O |         O |           O |       O |     O |     최소 |
| RTC 동기화      |     O |        제한 |           O |       X |    제한 |      X |
| SD 포맷        |     O |         X |           O |       X |    제한 |      X |

---

## 31. API/설정 기능 설계 초안

## 31.1 내부 설정 키 예시

```json
{
  "schema_version": 3,
  "device": {
    "name": "IMU-LOGGER-01",
    "timezone": "Asia/Seoul",
    "usb_mode": "msc_on_request"
  },
  "logging": {
    "sample_rate_hz": 1000,
    "segment_minutes": 5,
    "prealloc_mb": 64,
    "binary_format_version": 1,
    "sync_interval_sec": 5
  },
  "imu": {
    "gyro_range_dps": 2000,
    "accel_range_g": 16,
    "orientation": "top_mount",
    "auto_calibration": true
  },
  "power": {
    "auto_sleep_sec": 900,
    "motion_wake": true,
    "low_battery_shutdown": true,
    "sleep_mode": "light"
  },
  "ui": {
    "led_brightness": 32,
    "recording_led_policy": "minimal"
  },
  "wifi": {
    "enabled": false,
    "ap_ssid": "IMU-LOGGER",
    "ntp_sync_on_connect": true
  }
}
```

## 31.2 웹 API 예시

* `GET /api/status`
* `GET /api/config`
* `POST /api/config/validate`
* `POST /api/config/save`
* `POST /api/config/reload`
* `POST /api/config/default`
* `POST /api/config/export`
* `POST /api/config/import`
* `POST /api/record/start`
* `POST /api/record/stop`
* `POST /api/calibration/start`
* `POST /api/time/sync`
* `POST /api/sd/format`
* `GET /api/files`
* `GET /api/files/download?id=...`
* `GET /api/diagnostics`

## 31.3 API 응답 원칙

* 모든 응답은 `ok`, `code`, `message`, `data` 구조를 기본으로 함
* 설정 저장 API는 validation 결과와 changed fields를 함께 반환
* recording 중 변경 불가 항목은 `busy` 또는 `requires_idle` 코드 반환
* 설정 적용 후 재부팅 필요 항목은 `reboot_required=true` 반환

## 31.4 설정 변경 정책

* **즉시 적용 가능 항목**

  * LED 밝기
  * 장치 이름
  * 시간대
* **READY 상태에서만 적용 가능 항목**

  * sample_rate_hz
  * gyro/accel range
  * orientation
  * segment/prealloc 정책
* **재부팅 권장 항목**

  * USB 기본 모드 정책
  * 특정 low-level driver 옵션

## 31.5 설정 Import / Export 설계

### Export

* 현재 active config를 JSON으로 직렬화해 다운로드
* schema_version과 firmware version 포함

### Import

1. 업로드된 JSON parse
2. schema/version 확인
3. validation 수행
4. 현재 config와 diff 계산
5. 적용 가능 항목/불가 항목 분리
6. 사용자 승인 후 LittleFS 저장
7. 필요 시 reboot_required 표시

## 31.6 Factory Reset 설계

* `config.json`, `runtime.json`, `calib.json`, `stats.json` 초기화
* 기본값 재생성
* 사용자 로그 파일은 보존/삭제 정책 선택 가능
* 마지막 reset 원인 기록

---

## 32. 전체 기능 상세 설계

## 32.1 부팅~기록~종료 End-to-End 시나리오

```text
Power ON
→ Boot init
→ LittleFS mount
→ config.json load + validate + migrate
→ runtime/calib/stats load
→ self-test
→ RTC check
→ SD mount
→ IMU init
→ optional auto calibration
→ READY
→ Record start
→ file create + pre-allocate
→ IMU acquisition
→ double buffer logging
→ segment rotate
→ stop request
→ flush + finalize
→ stats update
→ READY or SLEEP
```

## 32.2 전체 기능 블록별 책임

### A. Boot/Init Block

* 하드웨어 bring-up
* 설정 로드
* self-test
* degraded boot 판단

### B. Configuration Block

* LittleFS mount
* JSON parse/serialize
* validation/migration
* commit/rollback

### C. Acquisition Block

* BMI270 init
* FIFO/interrupt handling
* timestamp attach
* sample sanity check

### D. Logging Block

* sample packing
* double/ring buffer 운용
* SD sequential write
* sync/latency monitoring

### E. File Management Block

* session id generation
* segment rotation
* header/footer 관리
* recovery scan

### F. UX Block

* button decode
* LED patterns
* 상태/오류 피드백

### G. Connectivity Block

* Wi-Fi AP/STA
* Web API
* USB MSC/CDC
* NTP sync

### H. Power/Safety Block

* battery monitor
* low voltage stop
* sleep/wake
* brownout 대응

### I. Diagnostics Block

* counters/statistics
* self-test results
* latency/error history
* service export

## 32.3 상태 기반 전체 기능 운용 정책

### BOOT

* 설정 로드만 허용
* 사용자 입력 최소 허용
* LED 부팅 패턴

### READY

* 기록 시작 가능
* 설정 수정 가능
* USB/Wi-Fi 진입 가능

### RECORDING

* 고속 취득 우선
* 설정 변경 제한
* Wi-Fi 대부분 제한
* USB는 stop 후 전환

### FLUSHING

* 사용자 강제 동작 최소화
* LED flush 표시
* file finalize 우선

### WIFI_CONFIG

* 웹 설정/상태조회/파일관리 가능
* 고속 기록과 동시 사용 비권장

### USB_MSC

* 내부 SD 접근 금지
* host detach 후 remount 필요

### ERROR

* 오류 코드 표시
* 복구 가능한 경우 recovery action 제시
* 치명 오류는 기록 금지

### SLEEP

* motion/button/USB wake 유지
* runtime state 최소 저장

## 32.4 전체 기능 간 충돌 정책

* recording 중 SD format 금지
* recording 중 config save는 runtime-safe 항목만 허용
* MSC 노출 중 file logging 금지
* Wi-Fi AP 활성 중 고주파 로깅 시작 금지 또는 자동 해제
* low battery critical 시 모든 부가기능보다 flush/shutdown 우선

## 32.5 영속 저장 데이터 분리 정책

### LittleFS 저장

* config.json
* config.bak
* runtime.json
* calib.json
* stats.json

### SD 저장

* imu binary log files
* session summary
* exported diagnostics (선택)

### RAM only

* active buffers
* pending events
* current state context
* transient fault counters

## 32.6 서비스/현장 운용 기능

* 현장 사용자는 버튼만으로 start/stop 가능
* 고급 설정은 웹 UI에서 관리
* 장애 발생 시 LED 패턴과 diagnostics로 빠르게 원인 파악
* 서비스 엔지니어는 stats/calib/config export로 분석 가능

## 32.7 상용 수준 필수 보강 포인트

* config 저장 atomicity 확보
* file finalize/recovery 완성
* SD card benchmark 기반 권장 카드 정책 마련
* 장시간 soak test 수행
* brownout/강제분리/USB attach stress test 필수
* schema migration 장기 유지 전략 수립

---

## 33. 진단/서비스 기능 설계

## 32.1 Factory Test Mode

* IMU read test
* SD R/W benchmark
* RTC set/check
* LED all-color test
* button matrix test
* battery ADC test
* USB enumeration test

## 32.2 Service Diagnostics

* 최근 부팅 횟수
* 비정상 종료 횟수
* 마지막 SD write max latency
* buffer high watermark
* dropped sample count
* calibration fail count
* brownout count
* firmware version / config version

---

## 34. 권장 클래스 책임 분리

### 34.1 AppController

* 전체 초기화
* main loop/event loop orchestration

### 34.2 SystemStateMachine

* 상태 전이 관리
* 상태별 허용 기능 판단

### 34.3 ImuManager

* BMI270 설정
* 샘플링 enable/disable
* FIFO read
* calibration apply

### 34.4 LoggerService

* sample pack
* buffer swap
* file write dispatch

### 34.5 FileSegmentManager

* 파일 생성/분할/종료/복구
* pre-allocation
* header/footer 관리

### 34.6 ConfigManager

* LittleFS mount/unmount
* JSON load/save
* validation/migration
* backup/rollback
* runtime/calib/stats 분리 저장

### 34.7 PowerManager

* battery monitor
* sleep/wake 정책
* shutdown sequence

### 34.8 LedPatternManager

* 상태별 LED 패턴 렌더링
* 우선순위 충돌 해소

### 34.9 ButtonManager

* debounce
* event decoding
* user input queue publish

### 34.10 WebConfigService

* Wi-Fi/AP 관리
* REST API
* config save/load

### 34.11 UsbModeManager

* USB attach/detach 감지
* MSC/CDC 모드 전환

### 34.12 HealthMonitor

* latency, overflow, fault 감시
* recover/fatal 판정

---

## 35. 구현 순서 권장안

### Step 1. 최소 동작 경로

* BMI270 init
* raw sample 취득
* SD binary write
* 버튼 1개로 start/stop
* LED 1개 상태 표시

### Step 2. 실사용 안정화

* double buffer
* pre-allocate
* segment rotate
* footer/finalize
* recovery scan

### Step 3. 제품화 핵심

* state machine 정식 적용
* button manager
* led pattern manager
* battery protection
* sleep/wake

### Step 4. 관리 기능

* RTC/NTP
* config manager
* web UI
* USB MSC/CDC

### Step 5. 상용 보강

* diagnostics
* factory mode
* bench metrics
* compatibility test

---

## 36. 최종 설계 요약

이 제품의 기능 설계 핵심은 다음 한 줄로 정리할 수 있습니다.

**“센서 취득은 절대 끊기지 않게, 저장은 늦더라도 유실 없이, 사용자 조작은 단순하게, 오류와 전원 차단에는 강하게.”**

따라서 구현 시 실제로 가장 중요한 기능 묶음은 아래 5가지입니다.

* **고속 IMU acquisition 경로**
* **double/ring buffer 기반 로깅 경로**
* **segment + pre-allocation + recovery 기반 파일 관리**
* **state machine 기반 시스템 제어**
* **battery/error/USB/Wi-Fi를 포함한 운영 안정성 제어**

다음 단계에서는 이 기능 설계를 바탕으로 바로 아래 3가지를 이어서 구체화할 수 있습니다.

1. **상세 클래스/모듈 구조도**
2. **FreeRTOS Task/Queue/Buffer 상세 설계**
3. **Binary Log 포맷(.bin) 상세 명세**







AP10_AppController
ST10_SystemStateMachine
ImuManager
LoggerService
FileManager
ConfigManager
LedManager
ButtonManager
PowerManager
WebConfigService
UsbManager
DiagnosticsService