/* ============================================================================
 * [SMEA-100 핵심 구현 원칙 및 AI 셀프 회고 바이블]
 * 본 주석은 프로젝트 전반에 걸쳐 절대 삭제되어서는 안 되며,
 * 코드를 수정/확장할 때마다 항상 최신 상태로 유지하고 점검해야 합니다.
 * ============================================================================
 *
 * [1. 시스템 아키텍처 및 하드웨어 구현 원칙]
 * 1. 매직넘버 철폐: 모든 하드웨어 핀, 태스크 스택, 임계값은 T410_Config_001.hpp의 
 * SmeaConfig 네임스페이스 내에 inline constexpr로 한 곳에서만 정의 및 관리한다.
 * 2. RTOS 듀얼코어 파이프라인: 
 * - Core 0: I2S DMA 데이터 수집 및 가벼운 RMS 트리거 검사 (ISR 폭풍 방어).
 * - Core 1: FSM 제어, 고부하 DSP(SIMD) 연산, 피처 추출, 데이터 로깅.
 * 3. Zero-Copy 메모리 풀: 대형 구조체(FeatureSlot)는 전역 풀에 정적으로 할당하고, 
 * RTOS Queue에는 1바이트 인덱스(slot_idx)만 넘겨 스택 고갈과 덮어쓰기(Race Condition)를 원천 차단한다.
 * 4. 16-Byte 정렬(SIMD 최적화): esp-dsp 가속기의 128-bit 처리 특성을 극대화하기 위해 
 * 모든 연산 버퍼 및 필터 계수는 반드시 alignas(16)으로 메모리를 정렬한다.
 * 5. 라이브러리 및 환경: PlatformIO Arduino Core 환경을 기반으로 하며, 
 * JSON 직렬화는 ArduinoJson V7.4.x 최신 규격을 엄격히 준수한다.
 *
 * ----------------------------------------------------------------------------
 * [2. AI 가 자주 반복하는 실수 및 방어 원칙 (Self-Reflection)]
 * 1. (실수) 필터 제자리(In-place) 연산 오염 
 * -> (방어) FIR/IIR 필터는 반드시 입출력 버퍼를 분리(핑퐁)하거나, 시계열이 유지되도록 역순 루프(Pre-emphasis)를 적용.
 * 2. (실수) 가속기 Over-read 패닉 
 * -> (방어) FFT 및 DSP 버퍼 차원은 무조건 4의 배수(16바이트)로 패딩(Padding) 할당.
 * 3. (실수) 상태 전이 시 과거 데이터(찌꺼기) 간섭 
 * -> (방어) FSM이 READY에서 MONITORING으로 넘어갈 때 무조건 resetFilterStates()를 호출하여 Delay Line 클리어.
 * 4. (실수) 네트워크 전송/파일 저장 시 페이로드 절단 
 * -> (방어) 바이트 배열이 아닌 Float 배열 전송 시, 길이는 항상 '요소 개수 * sizeof(float)'로 바이트 규격을 맞춤.
 * 5. (실수) FreeRTOS 태스크 파괴 시 데드락 발생 
 * -> (방어) vTaskDelete 호출 시, 생명주기 꼬임을 막기 위해 본인 핸들을 반드시 nullptr로 초기화.
 * 6. (실수) C++ -ffast-math 최적화로 인한 예외처리 무력화 
 * -> (방어) 부동소수점 0 나누기 방어는 fabsf(val) < 1e-6f 검사를 쓰고, NaN/Inf 유입은 비트 마스킹으로 하드웨어 컷.
 * 7. (실수) 큐에 남은 구형 데이터로 인한 정합성 붕괴
 * -> (방어) FSM 가동 시작 시 반드시 xQueueReset을 호출하여 잔여 인덱스를 깨끗이 비울 것.
 *
 * ----------------------------------------------------------------------------
 * [3. 기능 축소/누락 점검 및 보완 원칙 (Anti-Reduction)]
 * 1. 전체 맥락 유지: 코드 리팩토링이나 새로운 블록 작성 시, 기존에 확정된 요구사항
 * (예: 39D MFCC, 8/42kHz 호환성, N-top Peak, Delta 연산 등)이 은연중에 생략되거나 축소되지 않았는지 스스로 3회 교차 검증한다.
 * 2. 완성체 지향: 불필요한 더미(Dummy) 코드나 '이곳에 구현하세요' 식의 뼈대만 제공하는 것을 지양하고, 
 * 상황이 허락하는 한 즉시 컴파일 및 동작이 가능한 '풀버전(Full-version) 완성체'를 작성한다.
 * 3. 파이프라인 정합성 점검: 마이크 수집(I2S) -> 전처리(DSP) -> 특징추출(MFCC) -> 판정(Rule/ML) -> 
 * 저장(SDMMC/JSON)으로 이어지는 데이터의 흐름 중 단 하나의 연결고리도 끊어지지 않도록 입출력 데이터 타입과 길이를 일치시킨다.
 *
 * ----------------------------------------------------------------------------
 * [4. 미구현(TODO) 및 주의사항 주석 관리 원칙]
 * 1. 상태 보존: 현재 미구현된 블록(예: ML/TFLite 판정부 연동, 고도화 ANC 스펙트럼 차감 수식 등)의 
 * 주석을 함부로 삭제하여 기능이 이미 완성된 것처럼 시스템을 위장하지 않는다.
 * 2. 태그 규약: 작성 중이거나 추후 고도화가 필요한 부분은 반드시 "[TODO: 모듈명 - 상세 구현 과제 및 수학적 접근법]" 
 * 형태로 명시하여, 다음 개발 세션에서 문맥의 끊김 없이 즉시 이어서 코딩할 수 있게 한다.
 * 3. 경고 보존: 특정 필터 계수의 민감도, 하드웨어(PSRAM 캐시 미스 등)의 치명적 제약사항, 
 * 오버랩 구간의 시계열 처리 주의점 등은 해당 로직 바로 위에 "[WARNING: ...]" 태그로 남긴다. 
 * 이러한 경고성 주석은 소스코드가 변경되더라도 절대 지우지 않으며 항상 로직과 동기화하여 최신 상태로 관리한다.
 * ============================================================================ */
